[Unit]
Description=Start memocadre using DRM/KMS
After=network.target

[Service]
Environment=CONFIG_PATH=/etc/memocadre/config
Environment=SETTINGS_PATH=/etc/memocadre/settings
ExecStart=/usr/bin/memocadre
Group=video
User=memocadre
Restart=always

[Install]
WantedBy=multi-user.target
